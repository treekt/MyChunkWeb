spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/mychunk2
spring.datasource.username = root
spring.datasource.password =
spring.datasource.tomcat.connection-properties=useUnicode=true;characterEncoding=utf-8;

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update


spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

jsonapi.urlMain = http://localhost:20059/api/2/call
jsonapi.usernameMain = admin
jsonapi.passwordMain = changeme
jsonapi.saltMain = saltgoeshere

jsonapi.urlHub = http://localhost:20060/api/2/call
jsonapi.usernameHub = admin
jsonapi.passwordHub = changeme
jsonapi.saltHub = saltgoeshere
